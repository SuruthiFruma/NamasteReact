This episode is about optimising the app. Single Responsibility Principle Every component or a module must have a single purpose. Custom Hooks In react we can have our own custom hook.It is a good practice to start the custom hook with "use" especially when the project uses Linter which throws exception Explained creating a own custom hook for restaurant menu and one to detect online and offline status Lazy Loading It is also known as chunking, code splitting, dynamic bundling ,on demand loading. Importing using lazy and Suspense fallback for components is explained